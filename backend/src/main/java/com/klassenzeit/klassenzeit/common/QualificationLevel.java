package com.klassenzeit.klassenzeit.common;

/** Qualification level for teacher-subject assignments. */
public enum QualificationLevel {
  /** Teacher's primary subject with full certification. */
  PRIMARY,

  /** Secondary subject the teacher can teach. */
  SECONDARY,

  /** Can substitute but not primary assignment. */
  SUBSTITUTE
}
