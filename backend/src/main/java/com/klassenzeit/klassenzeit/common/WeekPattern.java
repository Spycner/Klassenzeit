package com.klassenzeit.klassenzeit.common;

/** Week pattern for lessons (supports A/B week rotation). */
public enum WeekPattern {
  /** Lesson occurs every week. */
  EVERY,

  /** Lesson occurs only in A-weeks. */
  A,

  /** Lesson occurs only in B-weeks. */
  B
}
