<?xml version="1.0"?>
<ruleset name="Klassenzeit PMD Rules"
    xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>
        PMD ruleset for Klassenzeit backend.
        Covers best practices, code style, design, and error-prone patterns.
    </description>

    <!-- Exclude Spring Boot Application classes from utility class check -->
    <exclude-pattern>.*Application\.java</exclude-pattern>

    <!-- Best Practices -->
    <rule ref="category/java/bestpractices.xml">
        <exclude name="GuardLogStatement"/>
        <exclude name="UnitTestShouldIncludeAssert"/>
        <!-- Integration tests validating multiple properties need multiple asserts -->
        <exclude name="UnitTestContainsTooManyAsserts"/>
        <!-- BaseEntity and AbstractIntegrationTest use abstract for design, not polymorphism -->
        <exclude name="AbstractClassWithoutAbstractMethod"/>
    </rule>

    <!-- Code Style -->
    <rule ref="category/java/codestyle.xml">
        <exclude name="AtLeastOneConstructor"/>
        <exclude name="CommentDefaultAccessModifier"/>
        <exclude name="ConfusingTernary"/>
        <exclude name="LocalVariableCouldBeFinal"/>
        <exclude name="MethodArgumentCouldBeFinal"/>
        <exclude name="OnlyOneReturn"/>
        <exclude name="ShortVariable"/>
        <exclude name="LongVariable"/>
        <exclude name="TooManyStaticImports"/>
        <!-- JPA entities require no-arg constructors -->
        <exclude name="UnnecessaryConstructor"/>
        <exclude name="CallSuperInConstructor"/>
        <!-- canTeachGrades is a valid name for a list of gradeable grades -->
        <exclude name="LinguisticNaming"/>
        <!-- Short class names like Term are acceptable for domain entities -->
        <exclude name="ShortClassName"/>
    </rule>

    <!-- Design -->
    <rule ref="category/java/design.xml">
        <exclude name="LawOfDemeter"/>
        <exclude name="LoosePackageCoupling"/>
        <exclude name="TooManyMethods"/>
        <exclude name="ExcessiveImports"/>
        <exclude name="UseUtilityClass"/>
        <!-- JPA entities are intentionally data classes -->
        <exclude name="DataClass"/>
        <!-- AbstractIntegrationTest uses abstract for test inheritance pattern -->
        <exclude name="AbstractClassWithoutAnyMethod"/>
        <!-- Testcontainers pattern requires mutable static container reference -->
        <exclude name="MutableStaticState"/>
    </rule>

    <!-- Error Prone -->
    <rule ref="category/java/errorprone.xml">
        <exclude name="MissingSerialVersionUID"/>
        <!-- Boolean fields like isActive with isActive() getters are standard Java convention -->
        <exclude name="AvoidFieldNameMatchingMethodName"/>
    </rule>

    <!-- Multithreading -->
    <rule ref="category/java/multithreading.xml">
        <exclude name="UseConcurrentHashMap"/>
    </rule>

    <!-- Performance -->
    <rule ref="category/java/performance.xml"/>

    <!-- Security -->
    <rule ref="category/java/security.xml"/>
</ruleset>
