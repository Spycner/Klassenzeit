<?xml version="1.0" encoding="UTF-8"?>
<FindBugsFilter
    xmlns="https://github.com/spotbugs/filter/3.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="https://github.com/spotbugs/filter/3.0.0 https://raw.githubusercontent.com/spotbugs/spotbugs/3.1.0/spotbugs/etc/findbugsfilter.xsd">

    <!--
        Exclude EI_EXPOSE_REP and EI_EXPOSE_REP2 (may expose internal representation)
        for JPA entity classes. JPA requires mutable entity relationships - defensive
        copies are not appropriate for entity getters/setters.
    -->
    <Match>
        <Bug pattern="EI_EXPOSE_REP,EI_EXPOSE_REP2"/>
    </Match>

    <!--
        Exclude SE_BAD_FIELD for Spring Security Authentication implementations.
        These extend JwtAuthenticationToken which implements Serializable, but in our
        stateless JWT-based auth flow, we never serialize authentication objects.
    -->
    <Match>
        <Class name="com.klassenzeit.klassenzeit.security.CurrentUserAuthentication"/>
        <Bug pattern="SE_BAD_FIELD"/>
    </Match>

</FindBugsFilter>
