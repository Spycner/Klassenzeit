[package]
name         = "solver-core"
version      = "0.1.0"
publish      = false
edition.workspace      = true
rust-version.workspace = true

[lib]
# default rlib

[dev-dependencies]
proptest = { workspace = true }
